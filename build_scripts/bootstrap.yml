---
- hosts: localhost
  become: yes

  tasks:

    - name: install basic tools git and curl
      apt:
        name: "{{ item }}"
        state: present
        # cache already updated by ansible installation
        update_cache: no
      with_items:
        - git
        - curl

    # npm is required by elasticdump and is not part of Debian stretch
    # repositories so we have to install it manually;
    # we install it through the nodejs package from node repo

    - name: add node repositories for npm installation
      shell: curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -

    - name: install npm (required by elasticdump)
      apt:
        name: nodejs
        state: present

    - name: create project directory
      file:
        path: /opt/tornado-kibana-logs-handler
        state: directory

    - name: clone project
      git:
        repo: https://github.com/jean553/tornado-kibana-logs-handler.git
        dest: /opt/tornado-kibana-logs-handler

    - name: install pip packages tox
      pip:
        name="tox"
        state=latest

    - name: create tox environment
      shell: >
        tox -r -e devdocker
      args:
        chdir: "/opt/tornado-kibana-logs-handler"
        creates: "/tmp/virtual_env35"

    - name: install elasticdump using npm
      npm:
        name: elasticdump
        global: yes
